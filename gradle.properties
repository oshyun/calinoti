# Gradle JVM 힙 설정: 안드로이드 빌드에 넉넉한 크기
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true

# 안드로이드 라이브러리는 AndroidX 기반만 사용한다
android.useAndroidX=true
android.nonTransitiveRClass=true
